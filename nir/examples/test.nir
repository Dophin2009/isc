struct A {
    pub n: u32,
    pub name: [char]
}

fn main() {
    let a: u32 = 5;
    let b: [u32] = [1, 2, 5];
    b[0] = 5;

    let c: str = "aaaaa";
    let d: u32 = b[1];

    for n: u32 in b {
        io::println(n);
    }
}

pub fn add(a: u32, b: u32) -> u32 {
    bye a + b;
}

